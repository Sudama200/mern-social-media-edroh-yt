export const isNonMobileScreens = useMediaQuery("(min-width: 1000px)");

export const neutralLight = theme.palette.neutral.light;
export const dark = theme.palette.neutral.dark;
export const background = theme.palette.background.default;
export const primaryLight = theme.palette.primary.light;
export const alt = theme.palette.background.alt;

export const fullName = `${user.firstName} ${user.lastName}`